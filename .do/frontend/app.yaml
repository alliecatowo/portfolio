domains:
  - domain: allisons.dev
    type: PRIMARY
    zone: allisons.dev
ingress:
  rules:
    - component:
        name: frontend
      match:
        path:
          prefix: /
name: allisons-portfolio-frontend
region: sfo
services:
  - build_command: |
      # Handle rollup native dependency explicitly
      rm -rf node_modules package-lock.json
      npm install --omit=optional
      # Explicitly install the needed Linux-specific Rollup binding
      npm install --no-save @rollup/rollup-linux-x64-gnu
      # Now build
      npm run build
    environment_slug: node-js
    envs:
      - key: NODE_ENV
        scope: BUILD_TIME
        value: production
      - key: NODE_OPTIONS
        scope: BUILD_TIME
        value: --unhandled-rejections=strict
      - key: NUXT_PUBLIC_DIRECTUS_URL
        scope: RUN_TIME
        value: https://allisons-portfolio-directus-9vxdi.ondigitalocean.app
      - key: NUXT_PUBLIC_API_URL
        scope: RUN_TIME
        value: https://allisons-portfolio-directus-9vxdi.ondigitalocean.app
      - key: NUXT_PUBLIC_DEV_SITE_URL
        scope: RUN_TIME
        value: https://allisons.dev
      - key: NUXT_PUBLIC_TATTOO_SITE_URL
        scope: RUN_TIME
        value: https://tattoo.allisons.dev
    git:
      branch: deploy/digital-ocean
      repo_clone_url: https://github.com/alliecatowo/portfolio.git
    http_port: 3000
    instance_count: 1
    instance_size_slug: basic-xs
    name: frontend
    run_command: node .output/server/index.mjs
    source_dir: frontend
