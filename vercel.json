{
    "buildCommand": "cd frontend && rm -rf node_modules package-lock.json && npm install && npm install @rollup/rollup-linux-x64-gnu @esbuild/linux-x64 && npm run generate && mkdir -p ../public && (cp -r .output/public/* ../public/ || cp -r .vercel/output/static/* ../public/)",
    "outputDirectory": "public",
    "framework": "nuxtjs",
    "routes": [
        {
            "src": "/gallery",
            "dest": "/tattoo/gallery"
        }
    ],
    "env": {
        "NUXT_PUBLIC_API_URL": "https://allisons-portfolio-directus-9vxdi.ondigitalocean.app",
        "NUXT_PUBLIC_DIRECTUS_TOKEN": "2eEMQA40l35OBtWNH6nDS166k0o800sb"
    },
    "git": {
        "deploymentEnabled": {
            "main": true,
            "development": true,
            "production": false
        }
    },
    "github": {
        "enabled": true,
        "silent": false
    }
}