{
    "name": "portfolio",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": "22.x"
    },
    "scripts": {
        "build": "pnpm --filter portfolio-frontend build",
        "dev": "pnpm --filter portfolio-frontend dev",
        "generate": "pnpm --filter portfolio-frontend generate",
        "preview": "pnpm --filter portfolio-frontend preview"
    },
    "devDependencies": {
        "nuxi": "^3.28.0"
    }
}